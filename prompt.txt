you are going to create me now the game session flow

using firestore snapshot + dart stream + stream builder

1. first after the user clicked the session tab add another interface there unlike the current state where it shows 8 different topics

1.2 instead show them two options first CREATE and JOIN

1.3 after the user clicked create let the user choose what topic he wants

1.4 after choosing the UI will await until the system was able to fetch 10 cards from that specific topic

1.5 if the 10 cards has been fetch 

1.6 host will be transfered to waiting interface where you will display a Alphanumeric 6 code generated on service side 

1.7 write it now on session document so that other player will be able to join the session through code user the enum status created

1.71 if the user cancled the lobby update the status to cancelled

1.8 the users present in the waiting lobby will be displayed in a container 4 x dynamic mkae the container not visible to eye

1.9 after the host started the session update the status on firestore to active 

2.0 SESSION FLOW: 
Server selects 10 cards 
        │
Participants receive the same card sequence
        │
Each comparison has a 15-second timer ( 15 sec x 10 = 2 min 30 sec ) 
        │
Swipe loser away
        │
Timer resets
        │
If timeout → auto-swipe (counts as timeout)

        │
First valid finisher (or top finishers) earns a shield
        │
Everyone finishes
        │
Server tallies X marks
        │
Shield deducts 1 X from the protected final card
        │
Show session results (map it to aggregated results in session entity)




2.1 implement this to random challenge for now to make debugging easier

